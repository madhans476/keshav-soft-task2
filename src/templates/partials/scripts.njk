{# src/templates/partials/scripts.njk
   Common end-of-body scripts every page needs. Page-specific scripts
   (like the contact form validation) are added via the layout's
   {% block extra_scripts %} instead of duplicating this file. #}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
